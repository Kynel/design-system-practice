{
  "name": "design-token",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx token-transformer --expandTypography --expandShadow ./tokens/tokens.json ./build/tokens.json global && style-dictionary build ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
